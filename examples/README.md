# Examples

All files here are **fictional**. They exist so an agent can see shape without touching real prospects.

| File | Use |
|------|-----|
| `copy-examples.md` | Reject/approve copy patterns |
| `relaydesk.company.md` | A filled company file (fake business) |
| `sample-leads.json` | Instantly lead shape — **do not send** |
| `sample-leads-linkedin.json` | HeyReach lead shape — **do not send** |

Real lists go in `../lists/` (gitignored). Real company context goes in `../company.md` (gitignored).
